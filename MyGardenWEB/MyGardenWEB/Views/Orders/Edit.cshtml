@model MyGardenWEB.Data.Order

@{
    ViewData["Title"] = "Edit";
}

<h1>Редактиране</h1>

<h4>Поръчки</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ProductsId" class="control-label">Продукт</label>
                <select asp-for="ProductsId" class="form-control" asp-items="ViewBag.ProductsId"></select>
                <span asp-validation-for="ProductsId" class="text-danger"></span>
            </div>
@*             <div class="form-group">
                <label asp-for="ClientsId" class="control-label">Клиент</label>
                <select asp-for="ClientsId" class="form-control" asp-items="ViewBag.ClientsId"></select>
                <span asp-validation-for="ClientsId" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <label asp-for="Quantity" class="control-label">Количество</label>
                <input asp-for="Quantity" class="form-control" min="1"/>
                <span asp-validation-for="Quantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Запази" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Връщане назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
